Fix the WEBEE dashboard visual shell shown in the attached screenshot.

The current problem is that the **top header, left sidebar, and main dashboard background look like separate blocks**, and the main blue is too light/flat.

I want the dashboard to match the deeper premium two-tone blue aesthetic used on **[www.webespokeai.com](http://www.webespokeai.com)** much more closely.

### Required changes

#### 1. Deepen the overall blue

Replace the current lighter blue background with a much deeper:

* midnight navy
* deep indigo
* subtle blue-violet

Use a **two-tone dark gradient**, not a flat colour.

The result should feel closer to:

`#071126 → #0B1430 → #17133C`

These are directional values only — inspect the existing website styles if they are already available in the codebase and reuse the closest existing WEBEE brand colours rather than introducing unnecessary new colours.

#### 2. Blend the top header into the dashboard

The top navigation bar currently looks like a separate dark strip.

Make it blend naturally into the main dashboard background.

Use:

* shared underlying colour tones
* subtle transparency
* very soft gradient transitions
* minimal border separation

Do NOT create a visibly separate header panel.

#### 3. Blend the sidebar

The left navigation currently looks too black and disconnected from the dashboard.

Bring it into the same deep navy/indigo colour family.

It should still be slightly darker for navigation hierarchy, but the transition into the main dashboard must be subtle.

Do not make it pure black.

#### 4. Remove harsh boundaries

Reduce or remove unnecessary hard:

* vertical divider lines
* horizontal divider lines
* contrasting background blocks
* obvious panel edges

Any remaining borders should be extremely subtle.

#### 5. Preserve the existing dashboard cards

Do not redesign the actual dashboard content.

Keep:

* stats
* agent pills
* voicemail banner
* live calls
* navigation
* icons
* spacing
* functionality

Only refine the **global shell, backgrounds and blending**.

#### 6. Preserve cyan accents

Keep the existing cyan/teal accents for:

* active navigation
* live agent indicators
* outlines
* status states
* interactive highlights

The cyan should stand out against the new deeper background.

Do NOT increase the amount of cyan in the background itself.

#### 7. Add extremely subtle depth

The main dashboard can have a very soft indigo illumination toward the upper-right/centre similar to the WEBEE website.

It must be subtle.

No obvious radial circles.
No bright blobs.
No cartoon glow.

It should feel like light naturally exists underneath the dark blue surface.

### Desired hierarchy

Visually it should feel like:

**Sidebar**
deepest navy

→ softly blends into →

**Main dashboard**
deep navy + restrained indigo two-tone gradient

→ softly blends into →

**Header**
same colour system with slight translucency

There should be no obvious point where one background ends and another begins.

### Important

Do NOT:

* change the overall dashboard layout
* change component positions
* alter functionality
* redesign the stats cards
* make everything purple
* add excessive gradients
* add glassmorphism everywhere
* add large background glows
* use pure black for the sidebar
* create visible containers around the page

This is a **refinement of the existing design**, not a redesign.

Use the current WEBEE website aesthetic as the visual source of truth.

Implement this properly in the shared shell/theme styles rather than adding page-specific CSS hacks.

After implementing, report:

1. files changed
2. previous background values
3. new background/gradient values
4. how the sidebar/header blending was improved
5. confirmation that no dashboard functionality or layout was changed
