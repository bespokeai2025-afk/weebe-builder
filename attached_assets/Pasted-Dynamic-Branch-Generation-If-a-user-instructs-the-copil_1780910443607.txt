Dynamic Branch Generation: If a user instructs the copilot to route a path that does not yet exist within the source node (e.g., "If they ask for a manager, route to agent transfer"), the engine must automatically update the source node to inject a brand new transition row named "Ask for manager", and then draw the connector wire to the target node.Collision-Aware Logical Positioning Engine: When creating a new node, the system must never drop it at a random default coordinate or pile it on top of existing items. It must execute the following positioning math:Sequential Generation Layout: If Node B is created as a step following Node A, look up Node A's coordinates $(x, y)$ and position Node B with an intentional horizontal offset to its right (e.g., $x + 320, y$).Branching / Stacked Layout: If Node A already has an outgoing connection branching to its right, calculate the layout vectors and stack the secondary new node below the existing target block vertically (e.g., $x + 320, y + 180$) to prevent overlapping nodes and intersecting lines.Floating Generation Layout: If a node is generated without immediate connections (e.g., "Add a floating note card"), check the current bounding coordinates of the user's active viewport view screen and position the node cleanly in the nearest open grid spacing.Fuzzy Semantic Matching: Implement a structural fallback matching utility. If the AI outputs a targeted node name or transition option that contains formatting or minor naming discrepancies compared to the canvas, map it to the closest semantic string match (e.g., matching "calendar function" to the active node named Check Availability).5. Design-Only Scope & Manual Deployment GuardrailStrict Sandbox Limit: The copilot must act purely as an arrangement, structural configuration, and editing tool. It is strictly forbidden from triggering live compilation, deployment pipelines, or interacting with the live "Deploy" or "Publish" functions.Draft State Preservation: Any panel settings changes spoken by the user (e.g., "Set the language to UK English, use Adrian's voice, and change the temperature to 0.4") must alter the visual form field elements within the right-hand draft settings panel only, keeping the agent safely inside an un-deployed state.Visual Confirmation: On command execution completion, show a subtle confirmation banner specifying the changes executed (e.g., "Successfully added 2 nodes and wired 1 transition path. Changes saved to Draft."), reminding the operator that live application updates must still be executed manually.