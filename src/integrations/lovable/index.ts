// OAuth is now handled directly via supabase.auth.signInWithOAuth()
// See src/routes/login.tsx and src/routes/signup.tsx for usage.
//
// Google OAuth configuration is done in the Supabase dashboard:
//   Authentication → Providers → Google → Enable
// and in Google Cloud Console for OAuth credentials.
